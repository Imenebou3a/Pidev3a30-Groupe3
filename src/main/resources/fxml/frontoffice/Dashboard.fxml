<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="tn.esprit.controllers.DashboardController"
            styleClass="main-container">

    <!-- HEADER -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="20" style="-fx-background-color: white; -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 2);">
            <Label text="Dashboard Administrateur" styleClass="title" style="-fx-font-size: 24px;"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="welcomeLabel" text="Bienvenue Admin" style="-fx-font-size: 14px; -fx-text-fill: #64748b;"/>
            <Button text="Mon Profil" onAction="#handleGoToProfil" styleClass="btn-secondary"/>
            <Button text="Déconnexion" onAction="#handleLogout" styleClass="btn-outline"/>
        </HBox>
    </top>

    <!-- CONTENU PRINCIPAL AVEC ONGLETS -->
    <center>
        <TabPane tabClosingPolicy="UNAVAILABLE" style="-fx-background-color: transparent;">

            <!-- ONGLET UTILISATEURS -->
            <Tab text="👥 Utilisateurs">
                <VBox spacing="20">
                    <padding>
                        <Insets top="30" bottom="30" left="40" right="40"/>
                    </padding>

                    <!-- STATISTIQUES UTILISATEURS -->
                    <HBox spacing="20" alignment="CENTER">
                        <VBox alignment="CENTER" spacing="10" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 30; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);" HBox.hgrow="ALWAYS">
                            <Label fx:id="totalUsersLabel" text="0" style="-fx-font-size: 36px; -fx-font-weight: bold; -fx-text-fill: #2563eb;"/>
                            <Label text="Total Utilisateurs" style="-fx-font-size: 14px; -fx-text-fill: #64748b;"/>
                        </VBox>

                        <VBox alignment="CENTER" spacing="10" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 30; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);" HBox.hgrow="ALWAYS">
                            <Label fx:id="totalAdminsLabel" text="0" style="-fx-font-size: 36px; -fx-font-weight: bold; -fx-text-fill: #ef4444;"/>
                            <Label text="Administrateurs" style="-fx-font-size: 14px; -fx-text-fill: #64748b;"/>
                        </VBox>

                        <VBox alignment="CENTER" spacing="10" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 30; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);" HBox.hgrow="ALWAYS">
                            <Label fx:id="totalHotesLabel" text="0" style="-fx-font-size: 36px; -fx-font-weight: bold; -fx-text-fill: #10b981;"/>
                            <Label text="Hôtes" style="-fx-font-size: 14px; -fx-text-fill: #64748b;"/>
                        </VBox>

                        <VBox alignment="CENTER" spacing="10" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 30; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);" HBox.hgrow="ALWAYS">
                            <Label fx:id="totalPendingLabel" text="0" style="-fx-font-size: 36px; -fx-font-weight: bold; -fx-text-fill: #f59e0b;"/>
                            <Label text="En Attente" style="-fx-font-size: 14px; -fx-text-fill: #64748b;"/>
                        </VBox>
                    </HBox>

                    <!-- BARRE DE RECHERCHE UTILISATEURS -->
                    <HBox spacing="15" alignment="CENTER_LEFT" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);">
                        <Button text="➕ Ajouter" onAction="#handleAddUser" styleClass="btn-success" prefWidth="150"/>
                        <Button fx:id="modifyButton" text="✏️ Modifier" onAction="#handleModifyUser" styleClass="btn-primary" prefWidth="150" disable="true"/>
                        <Separator orientation="VERTICAL"/>
                        <TextField fx:id="searchField" promptText="Rechercher par nom, prénom ou email..." styleClass="text-field" HBox.hgrow="ALWAYS" prefWidth="400"/>
                        <Button text="🔍 Rechercher" onAction="#handleSearch" styleClass="btn-primary"/>
                        <ComboBox fx:id="roleFilter" promptText="Filtrer par rôle" styleClass="combo-box" prefWidth="150"/>
                        <ComboBox fx:id="statutFilter" promptText="Filtrer par statut" styleClass="combo-box" prefWidth="150"/>
                        <Button text="↻ Réinitialiser" onAction="#handleReset" styleClass="btn-secondary"/>
                    </HBox>

                    <!-- TABLEAU UTILISATEURS -->
                    <VBox spacing="10" VBox.vgrow="ALWAYS" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);">
                        <Label text="Liste des Utilisateurs" style="-fx-font-size: 18px; -fx-font-weight: bold;"/>

                        <TableView fx:id="usersTable" VBox.vgrow="ALWAYS">
                            <columns>
                                <TableColumn fx:id="idColumn" text="ID" prefWidth="50"/>
                                <TableColumn fx:id="nomColumn" text="Nom" prefWidth="120"/>
                                <TableColumn fx:id="prenomColumn" text="Prénom" prefWidth="120"/>
                                <TableColumn fx:id="emailColumn" text="Email" prefWidth="200"/>
                                <TableColumn fx:id="telephoneColumn" text="Téléphone" prefWidth="100"/>
                                <TableColumn fx:id="roleColumn" text="Rôle" prefWidth="100"/>
                                <TableColumn fx:id="statutColumn" text="Statut" prefWidth="100"/>
                                <TableColumn fx:id="actionsColumn" text="Actions" prefWidth="280"/>
                            </columns>
                        </TableView>
                    </VBox>
                </VBox>
            </Tab>

            <!-- ONGLET RÉCLAMATIONS -->
            <Tab text="📋 Réclamations">
                <VBox spacing="20">
                    <padding>
                        <Insets top="30" bottom="30" left="40" right="40"/>
                    </padding>

                    <!-- STATISTIQUES RÉCLAMATIONS -->
                    <HBox spacing="20" alignment="CENTER">
                        <VBox alignment="CENTER" spacing="10" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 30; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);" HBox.hgrow="ALWAYS">
                            <Label fx:id="totalReclamationsLabel" text="0" style="-fx-font-size: 36px; -fx-font-weight: bold; -fx-text-fill: #2563eb;"/>
                            <Label text="Total" style="-fx-font-size: 14px; -fx-text-fill: #64748b;"/>
                        </VBox>

                        <VBox alignment="CENTER" spacing="10" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 30; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);" HBox.hgrow="ALWAYS">
                            <Label fx:id="reclamationsEnAttenteLabel" text="0" style="-fx-font-size: 36px; -fx-font-weight: bold; -fx-text-fill: #f59e0b;"/>
                            <Label text="En Attente" style="-fx-font-size: 14px; -fx-text-fill: #64748b;"/>
                        </VBox>

                        <VBox alignment="CENTER" spacing="10" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 30; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);" HBox.hgrow="ALWAYS">
                            <Label fx:id="reclamationsEnCoursLabel" text="0" style="-fx-font-size: 36px; -fx-font-weight: bold; -fx-text-fill: #3b82f6;"/>
                            <Label text="En Cours" style="-fx-font-size: 14px; -fx-text-fill: #64748b;"/>
                        </VBox>

                        <VBox alignment="CENTER" spacing="10" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 30; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);" HBox.hgrow="ALWAYS">
                            <Label fx:id="reclamationsResoluesLabel" text="0" style="-fx-font-size: 36px; -fx-font-weight: bold; -fx-text-fill: #10b981;"/>
                            <Label text="Résolues" style="-fx-font-size: 14px; -fx-text-fill: #64748b;"/>
                        </VBox>
                    </HBox>

                    <!-- FILTRES RÉCLAMATIONS -->
                    <HBox spacing="15" alignment="CENTER_LEFT" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);">
                        <ComboBox fx:id="reclamationStatutFilter" promptText="Filtrer par statut" styleClass="combo-box" prefWidth="180"/>
                        <ComboBox fx:id="reclamationCategorieFilter" promptText="Filtrer par catégorie" styleClass="combo-box" prefWidth="180"/>
                        <ComboBox fx:id="reclamationPrioriteFilter" promptText="Filtrer par priorité" styleClass="combo-box" prefWidth="180"/>
                        <Button text="↻ Réinitialiser" onAction="#handleResetReclamations" styleClass="btn-secondary"/>
                    </HBox>

                    <!-- TABLEAU RÉCLAMATIONS -->
                    <VBox spacing="10" VBox.vgrow="ALWAYS" style="-fx-background-color: white; -fx-background-radius: 12px; -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 15, 0, 0, 2);">
                        <Label text="Liste des Réclamations" style="-fx-font-size: 18px; -fx-font-weight: bold;"/>

                        <TableView fx:id="reclamationsTable" VBox.vgrow="ALWAYS">
                            <columns>
                                <TableColumn fx:id="reclamationIdColumn" text="ID" prefWidth="50"/>
                                <TableColumn fx:id="reclamationUtilisateurColumn" text="Utilisateur" prefWidth="150"/>
                                <TableColumn fx:id="reclamationSujetColumn" text="Sujet" prefWidth="200"/>
                                <TableColumn fx:id="reclamationCategorieColumn" text="Catégorie" prefWidth="120"/>
                                <TableColumn fx:id="reclamationStatutColumn" text="Statut" prefWidth="120"/>
                                <TableColumn fx:id="reclamationPrioriteColumn" text="Priorité" prefWidth="100"/>
                                <TableColumn fx:id="reclamationDateColumn" text="Date" prefWidth="150"/>
                                <TableColumn fx:id="reclamationActionsColumn" text="Actions" prefWidth="280"/>
                            </columns>
                        </TableView>
                    </VBox>
                </VBox>
            </Tab>

        </TabPane>
    </center>

</BorderPane>